# TripBalancing Quality Repair — 2026-08-21

This repair keeps the existing UI/theme/layout and targets the premium-guide data-quality problems found in the uploaded Mumbai, Varanasi, Bali and Baku PDFs/video.

1. Removed fabricated generic fallback attraction/food/hotel names from production fallback output.
2. Unknown destinations now fail safely with DESTINATION_DATA_UNVERIFIED instead of producing fake premium recommendations.
3. Added itinerary quality validation for generic placeholders, missing attractions/food and under-filled days.
4. Added curated offline fallback profiles for Mumbai, Varanasi, Bali and Baku.
5. Fallback itinerary days now contain destination-specific attraction + local-food activities (minimum two meaningful activities).
6. Added destination-aware route transport profiles (Bali no Metro; Varanasi e-rickshaw/auto; Mumbai Metro/local train where appropriate; Baku Metro/Bolt where appropriate).
7. Corrected accommodation calculations to use nights (days - 1) and rooms, including fallback hotel rate denominators.
8. Added transparent per-day cost breakdown (stay, food, local transport, activities, miscellaneous) reconciled to the deterministic budget engine.
9. Added Unicode/control-character sanitization for generated names and special handling for corrupted parenthetical hotel names.
10. Improved hotel PDF pagination with compact cards and orphan prevention; removed fabricated PDF-only fallback hotels.
11. Hotel cards now use destination/property-specific descriptions when supplied instead of one repeated template sentence.
12. AI Recommended Budget mode now shows a safe recommended budget (10% buffer) separately from the deterministic realistic estimate; PDF label changes accordingly.

Additional repairs:
- Added Bali/Indonesia emergency-number mapping and localized phrases for Bali and Baku.
- Renamed the misleading "Travel Advisory & Private Memories" PDF section to "Travel Advisory & Local Insights" because no private-memory feature is rendered there.
- PDF walking-distance/travel-time stats now derive from route data instead of activity-count formulas.
- Existing visual theme/layout was preserved.

Validation performed:
- All 39 TS/TSX source files passed TypeScript syntax transpilation diagnostics (0 syntax errors).
- Budget reconciler runtime smoke test passed, including nights-based accommodation, daily category allocation and AI recommended-vs-realistic separation.
- Full `npm run build` could not be executed in the sandbox because dependency installation did not complete within the environment timeout; no node_modules are included in this ZIP.

## Follow-up budget/PDF polish
- AI-budget mode now labels the difference between expected cost and recommended safe budget as **Recommended Safety Buffer**, never leftover money.
- PDF summary uses **AI RECOMMENDED** instead of **PLANNED** in AI-budget mode, and the financial intro explains the safety buffer.
- Daily cost breakdown components are integer-reconciled so they add exactly to each displayed daily total; accommodation remains limited to actual nights.
- PDF route cards now show total **Route Distance** and **Transit Time** from activity-to-activity legs instead of misleading walking-distance fallbacks.
- PDF text sanitization transliterates unsupported characters when using jsPDF built-in Helvetica, preventing Azerbaijani mojibake.
- Customer airfare fallback copy no longer exposes Travelpayouts/Aviasales/cache internals; it simply states that live fare is unavailable and the amount is a planning estimate.
