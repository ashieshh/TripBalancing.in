TripBalancing Step 6 launch audit - 2026-08-25

Baseline: TripBalancing.in-main(20260825-070345).zip

Verified in active code:
- Signup sends country_code and derived pricing_region in Supabase auth metadata.
- Server reads pricing region from user_profiles and maps IN -> INR, INTL -> USD.
- Razorpay plan amounts are server-defined, not browser-defined.
- Missing account region returns no currency rather than silently choosing a region.
- package scripts run server.ts; server-fixed.ts was not referenced by runtime.

Safe cleanup performed (no runtime imports found):
- removed server-fixed.ts (legacy duplicate containing obsolete test Razorpay fallback)
- removed src/components/DashboardOverview.tsx
- removed src/components/LiveFlightSearch.tsx
- removed src/services/budgetEngine.ts
- removed src/db/ (unused old Drizzle/Postgres code; package.json has no Drizzle/Postgres dependencies)

Not changed:
- UI/design
- plan prices
- India/International pricing logic
- Razorpay live checkout logic
- Gemini logic
- budget/PDF calculation
- Supabase RLS migrations

Build status:
- npm run build could not execute in this supplied archive because node_modules is not included (vite: not found).
- This is not reported as a successful production build.

Production launch checks still requiring the deployed environment:
1. India signup after email confirmation -> INR pricing.
2. Non-India signup after email confirmation -> USD pricing.
3. Live Razorpay INR payment and international-card USD payment.
4. Generate/save/reload a trip and export PDF.
5. Buddy/support/refund flows under production RLS if those features are enabled.
