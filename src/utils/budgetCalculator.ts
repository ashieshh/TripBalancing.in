// Backward-compatible export. All travel budget calculations now live in one
// canonical engine so feasibility, itinerary, charts and PDF use the same math.
export * from "../services/budgetEngine";
